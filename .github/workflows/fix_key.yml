name: Apply Supabase key
on:
  push:
    paths:
      - '.github/fix_key.py'
      - '.github/workflows/fix_key.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python .github/fix_key.py
      - run: python -m py_compile app.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app.py
          git diff --cached --quiet || git commit -m 'Configure publishable Supabase key'
          git push
