name: Patch login visual auto
on:
  push:
    paths:
      - '.github/patch_login_visual.py'
      - '.github/workflows/patch_login_visual_auto.yml'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply patch
        run: python .github/patch_login_visual.py
      - name: Validate
        run: python -m py_compile app.py
      - name: Commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app.py
          git diff --cached --quiet || git commit -m 'Ajusta tela de login e reutiliza logo configurada'
          git push
